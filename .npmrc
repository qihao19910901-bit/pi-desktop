# npm 缓存 → 项目内 F 盘
cache=F:/软件/我的秘籍/PI/.cache/npm

# Electron 二进制下载缓存（npm 安装 electron 包时用）
# 注：electron 包读取此环境变量，.npmrc 无法直接设，需在 npm scripts 里用 cross-env 设
# 见 package.json scripts

# 加快安装
fund=false
audit=false
